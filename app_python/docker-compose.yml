services:
  app:
    build: .
    image: devops-lab12-python:local
    container_name: app-python-lab12
    ports:
      - "5000:5000"
    environment:
      HOST: "0.0.0.0"
      PORT: "5000"
      VISITS_FILE: "/app/data/visits"
    volumes:
      - ./data:/app/data
