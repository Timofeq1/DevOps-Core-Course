---
common_packages:
  - python3-pip
  - python3-venv
  - curl
  - git
  - vim
  - htop
  - ca-certificates
  - gnupg
  - lsb-release

common_timezone: UTC

common_users:
  - name: "{{ ansible_user | default('ubuntu') }}"
